apiVersion: 1

providers:
  - name: 'Default'
    orgId: 1
    folder: ''
    type: file
    disableDeletion: false
    editable: true
    updateIntervalSeconds: 10  # 자동 업데이트 간격
    allowUiUpdates: true      # UI 업데이트 허용
    options:
      path: /etc/grafana/provisioning/dashboards/json 